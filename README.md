# Карта сетевой инфраструктуры

## Структура проекта

```
Map/
├── index.html          # Главная страница
├── main.js             # Монолитный файл (все функции в одном)
├── styles.css          # Стили приложения
├── README.md           # Этот файл
└── js/                 # Модульная версия (разделённый код)
    ├── config.js       # Глобальные переменные и конфигурация
    ├── utils.js        # Утилиты и вспомогательные функции
    ├── data.js         # Сохранение/загрузка данных
    ├── tracing.js      # Трассировка жил
    ├── nodeConnections.js  # Соединения кросс-узел
    └── index.js        # Главный файл модульной версии
```

## Использование

### Текущая версия (монолитная)
Используется файл `main.js` - все функции в одном файле.

### Модульная версия
Для использования модульной версии, замените в `index.html`:
```html
<script src="main.js"></script>
```
на:
```html
<script src="js/config.js"></script>
<script src="js/utils.js"></script>
<script src="js/data.js"></script>
<script src="js/tracing.js"></script>
<script src="js/nodeConnections.js"></script>
<script src="js/index.js"></script>
```

## Основные модули

### config.js
Глобальные переменные:
- `myMap` - объект карты Яндекс
- `objects` - массив всех объектов на карте
- `isEditMode` - режим редактирования
- `currentCableTool` - режим прокладки кабеля
- и другие...

### utils.js
Вспомогательные функции:
- `generateUniqueId()` - генерация уникального ID
- `getObjectUniqueId()` - получение ID объекта
- `escapeHtml()` - экранирование HTML
- `calculateDistance()` - расчёт расстояния
- `getCableColor()`, `getCableWidth()` - параметры кабеля
- `getFiberColors()` - цвета жил
- `getObjectTypeName()` - названия типов объектов

### data.js
Работа с данными:
- `saveData()` - сохранение в localStorage
- `loadData()` - загрузка из localStorage
- `importData()` - импорт данных
- `exportData()` - экспорт в файл
- `clearMap()` - очистка карты
- `updateStats()` - обновление статистики

### tracing.js
Трассировка жил:
- `traceFiberPathFromObject()` - трассировка от объекта
- `traceFromNode()` - трассировка от узла сети
- `showFiberTraceFromCross()` - отображение трассы
- `getNodeConnectedFibers()` - получение жил узла

### nodeConnections.js
Соединения кросс-узел:
- `connectFiberToNode()` - подключение жилы к узлу
- `disconnectFiberFromNode()` - отключение жилы
- `createNodeConnectionLine()` - создание визуальной линии
- `updateAllNodeConnectionLines()` - обновление всех линий

## Типы объектов

| Тип | Описание | Иконка |
|-----|----------|--------|
| `node` | Узел сети | 🖥️ |
| `support` | Опора связи | 📍 |
| `sleeve` | Кабельная муфта | 🔴 |
| `cross` | Оптический кросс | 📦 |
| `cable` | Кабель | — |

## Типы кабелей

| Тип | Жил | Цвет |
|-----|-----|------|
| `fiber4` | 4 | Красный |
| `fiber8` | 8 | Оранжевый |
| `fiber16` | 16 | Фиолетовый |
| `fiber24` | 24 | Бирюзовый |
| `copper` | 4 | Серый |

## Логика трассировки

1. **Узел сети** → подключается к кроссу через жилу
2. **Кросс** → соединяет жилы между кабелями или выводит на узел
3. **Муфта** → соединяет жилы между кабелями
4. **Опора** → кабель проходит через неё без изменений
5. **Кабель** → переносит жилы между объектами

## Валидация соединений

- ✅ Жила может быть соединена только с одной другой жилой
- ✅ Нельзя соединить жилы одного кабеля
- ✅ Жила подключенная к узлу не может быть соединена с другой жилой
- ✅ Кабель нельзя проложить напрямую к узлу сети
